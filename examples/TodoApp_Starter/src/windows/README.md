# TodoApp Windows Starter

Empty WinUI 3 and C# application for the TodoApp example.

```powershell
dotnet run --project TodoApp.csproj
```

Validate with:

```powershell
dotnet build TodoApp.slnx
dotnet format TodoApp.slnx --verify-no-changes
```